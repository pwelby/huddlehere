//  File: /views/partials/header.jade
//  Author: Evan O'Leary
//
//  The header of all pages is defined here.
//  Follows a boilerplate implementation found here: https://github.com/sahat/hackathon-starter/tree/master/views

.container 
  .row 
    //img (src="/images/HuddleHere_logo.png")
    h1 HuddleHere
    h3 &nbsp;Make Group Projects Easy!
    br
    br



//- For development purposes, easy navagation of pages. Also, maybe used in future releases when a login/ signup is set up.
    .navbar.navbar-default.navbar-fixed-top
    .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')
        i.fa.fa-cube
        | HuddleHere
     .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=(title == 'Home') ? 'active' : undefined)
          a(href='/') Meeting Creator
        li(class=(title == 'About') ? 'active' : undefined)
          a(href='/about') About
        li(class=(title == 'Useless') ? 'active' : undefined)
          a(href='/planner-leader') Planner (Leader View)
      //- ul.nav.navbar-nav.navbar-right
          if !user
          li(class=(title == 'Login') ? 'active' : undefined)
            a(href='/login') Login
          li(class=(title == 'Create Account') ? 'active' : undefined)
            a(href='/signup') Create Account
           else
           li.dropdown(class=(title == 'Account Management') ? 'active' : undefined)
            a.dropdown-toggle(href='#', data-toggle='dropdown')
              if user.profile.picture
                img(src='#{user.profile.picture}')
              else
                img(src='#{user.gravatar(60)}')
              | #{user.profile.name || user.email || user.id}&nbsp;
              i.caret
             ul.dropdown-menu
              li
                a(href='/account') My Account
              li.divider
              li
                a(href='/logout') Logout
                
                