//  File: /views/layout.jade
//  Author: Evan O'Leary
//
//  Serves as a base for all pages. Will display a header and a footer and replace block content specific to each page.
//  Follows a boilerplate implementation found here: https://github.com/sahat/hackathon-starter/tree/master/views

doctype html
html
  head
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.1/jquery.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.15.0/jquery.validate.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.15.0/additional-methods.min.js')
    script(src='/javascripts/jquery-ui.min.js')
    script(src='/bootstrap/js/bootstrap.min.js')
    //- script(src='/javascripts/timeutil.js')
    //- script(src='/javascripts/availRand.js')
    title #{title} - HuddleHere
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='stylesheet', href='/stylesheets/jquery-ui.min.css')
    link(rel='stylesheet', href='/stylesheets/jquery-ui.structure.min.css')
    link(rel='stylesheet', href='/stylesheets/jquery-ui.theme.min.css')
    link(rel='stylesheet', href='/bootstrap/css/bootstrap.min.css')
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css")
    
  body
    include partials/header
    
    .container 
      block content
      
    include partials/footer
    
    //For development purposes
    //-  div#navArea
        h3 ALPHA NAV:
        a(href='/') Meeting Creator
        a(href='/planner-leader') Planner (Leader View)
        a(href='/planner-member') Planner (Member View)
        a(href='/coach') Meeting Schedule Coach
    div(class='container', id='mainContainer')
      //- NOTE: This code block offers some utility functions to make parsing data from the API easier.
      //- These are available for use in evaluating #{} expressions in the Jade templates ONLY.
      //- 
        var parseTime = function(objDate) {
        var hours, minutes, ampm;  
        hours = objDate.getHours();
        if (hours < 12) ampm = "AM";
        else ampm = "PM";
        if (hours === 0) hours = "12";
        else if (hours > 12) hours = hours % 12;  
        minutes = objDate.getMinutes();
        if (minutes === 0) minutes = "00";
        else if (minutes < 10) minutes = "0" + minutes;   
        return "" + hours + ":" + minutes + " " + ampm;
        };
    
